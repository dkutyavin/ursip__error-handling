{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.18.4"
  },
  "scripts": {
    "build": "lerna run --stream build",
    "start": "lerna run --stream start"
  }
}
